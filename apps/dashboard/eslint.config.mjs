// The dashboard is linted by the repository-wide eslint config. Next's own
// lint step is disabled during build because it would need a second, partly
// overlapping rule set; `npm run lint` at the root covers this app.
export default [];
